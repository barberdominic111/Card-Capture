import React from "react";
import ReactDOM from "react-dom/client";
import CardCapture from "./CardCapture.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CardCapture />
  </React.StrictMode>
);
